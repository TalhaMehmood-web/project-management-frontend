import NavbarWrapper from "./navbar-wrapper";
import NotificationBellIcon from "./notification-bell.icon";
const Navbar = () => {
  return (
    <NavbarWrapper className="sticky top-0 z-20">
      <NotificationBellIcon />
    </NavbarWrapper>
  );
};

export default Navbar;
